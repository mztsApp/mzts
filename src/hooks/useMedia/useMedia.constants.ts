const ALLOWED_BREAKPOINTS = {
  MIN_SM: '(min-width: 640px)',
  MIN_MD: '(min-width: 768px)',
  MIN_LG: '(min-width: 1024px)',
  MIN_XL: '(min-width: 1280px)',
} as const;

export { ALLOWED_BREAKPOINTS };
